---
phase: 02-english-extraction
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/GenerationDialog.tsx
  - frontend/src/components/ResultViewer.tsx
  - frontend/src/components/SponsorDialog.tsx
  - frontend/src/components/AiProviderDialog.tsx
  - frontend/src/components/ForcedSetupGate.tsx
  - frontend/src/components/settings/ProviderSetupModal.tsx
  - frontend/src/i18n/locales/en.json
  - frontend/src/i18n/locales/tr.json
autonomous: true
requirements:
  - EXTRACT-01
  - EXTRACT-02
  - EXTRACT-06
  - EXTRACT-07
tags:
  - i18n
  - react-i18next
  - dialogs
must_haves:
  truths:
    - "GenerationDialog (video/image/character/storyboard/prompt) renders in English from t() calls"
    - "ResultViewer renders in English from t() calls; formatRelativeTime continues to use Phase 1 time.* keys"
    - "All Vietnamese strings in GenerationDialog (placeholders, progress text, validation, infotips) are gone — replaced with English values stored under dialog.* keys"
    - "Brand identifiers (Veo, Omni Flash, Banana Pro, Banana 2, Nano Banana Pro, etc.) never appear as keys or values in en.json"
    - "data.title and other user data is never wrapped"
    - "npm run lint passes"
  artifacts:
    - path: "frontend/src/components/GenerationDialog.tsx"
      provides: "Dialog title/labels/placeholders/infotips extracted; Vietnamese replaced with English"
      contains: "useTranslation"
    - path: "frontend/src/components/ResultViewer.tsx"
      provides: "Metadata grid, action buttons, blocked-state messages extracted (formatRelativeTime unchanged from Phase 1)"
      contains: "useTranslation"
    - path: "frontend/src/i18n/locales/en.json"
      provides: "~80 new dialog.* + result.* + sponsor.* + provider.* keys appended"
      contains: "dialog.title_video"
  key_links:
    - from: "frontend/src/components/GenerationDialog.tsx"
      to: "frontend/src/i18n/locales/en.json"
      via: "t(\"dialog.*\") calls; brand strings stay in constants imports"
      pattern: "t\\(\"dialog\\."
    - from: "frontend/src/components/ResultViewer.tsx"
      to: "frontend/src/i18n/locales/en.json"
      via: "t(\"result.*\") calls; existing time.* utility from Phase 1 untouched"
      pattern: "t\\(\"result\\."
---

<objective>
Extract every translatable string in the GenerationDialog, ResultViewer, and the smaller dialog/gate components. This plan covers the second-largest extraction surface (~80 rows). GenerationDialog has the heaviest Vietnamese-to-English rewriting load — multiple placeholders and infotips currently render Vietnamese strings to all users.

Purpose: Satisfy EXTRACT-01 (JSX text nodes), EXTRACT-02 (JSX attribute props), EXTRACT-06 (brand stays out of catalog), and EXTRACT-07 (user data never wrapped) for the dialog/viewer surface.

Output:
- GenerationDialog, ResultViewer, SponsorDialog, AiProviderDialog, ForcedSetupGate, and settings/ProviderSetupModal use `useTranslation()` + `t("dialog.*"|"result.*"|"sponsor.*"|"provider.*")`
- Every Vietnamese string in the inventory (`Bỏ trống...`, `Quốc gia`, `Mô tả thêm`, `Đang phân tích`, etc.) is replaced with English copy stored under the proposed key
- en.json gains ~80 new keys; tr.json mirrors with empty-string stubs
- `npm run lint` exits 0
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-english-extraction/02-CONTEXT.md
@.planning/phases/01-infra-audit/STRING-INVENTORY.md
@.planning/phases/01-infra-audit/01-01-SUMMARY.md

# i18n surface anchor
@frontend/src/i18n/i18n.ts
@frontend/src/i18n/locales/en.json
@frontend/src/i18n/locales/tr.json

# Files to modify (read ONCE)
@frontend/src/components/GenerationDialog.tsx
@frontend/src/components/ResultViewer.tsx
@frontend/src/components/SponsorDialog.tsx
@frontend/src/components/AiProviderDialog.tsx
@frontend/src/components/ForcedSetupGate.tsx
@frontend/src/components/settings/ProviderSetupModal.tsx
</context>

<append_only_contract>
Same as 02-01: APPEND-only to en.json and tr.json. Keys grouped under `dialog.*`, `result.*`, `sponsor.*`, `provider.*`. Never delete or reorder existing keys. One key per line. Every en key has a matching empty-string tr key.
</append_only_contract>

<tasks>

<task type="auto">
  <name>Task 1: Add useTranslation imports + hook calls to all six dialog/viewer components</name>
  <files>frontend/src/components/GenerationDialog.tsx, frontend/src/components/ResultViewer.tsx, frontend/src/components/SponsorDialog.tsx, frontend/src/components/AiProviderDialog.tsx, frontend/src/components/ForcedSetupGate.tsx, frontend/src/components/settings/ProviderSetupModal.tsx</files>
  <read_first>
    - STRING-INVENTORY.md sections for each of the six files (rows count: GenerationDialog ~42 + ~8 do-not-translate, ResultViewer ~30 + ~12 do-not-translate, SponsorDialog 2, AiProviderDialog 5, ForcedSetupGate 0, ProviderSetupModal 3)
    - ResultViewer.tsx already has `useTranslation` (Phase 1 wired it for formatRelativeTime) — VERIFY this before adding a duplicate import
  </read_first>
  <action>
    For each of the six files that contain translatable rows (ForcedSetupGate has none, so skip it for Task 1):
    - Add `import { useTranslation } from "react-i18next";` if not already present
    - Add `const { t } = useTranslation();` at the top of the primary exported component (just below the function signature, before any other hook)

    ResultViewer.tsx already has `const { t, i18n } = useTranslation()` from Phase 1's formatRelativeTime rewrite — do NOT add a duplicate. Verify by `grep -n "useTranslation" frontend/src/components/ResultViewer.tsx` before editing.

    For ForcedSetupGate.tsx, the inventory says "No user-visible strings (delegates all rendering to AiProviderDialog)" — touch this file ONLY if a manual re-read finds a missed row. If clean, leave it out of Task 1; the orchestrator's grep gate covers verification.

    No dynamic key construction. Every `t("...")` uses a literal key per i18n.ts file header rules and CONTEXT.md.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; for f in src/components/GenerationDialog.tsx src/components/ResultViewer.tsx src/components/SponsorDialog.tsx src/components/AiProviderDialog.tsx src/components/settings/ProviderSetupModal.tsx; do grep -q "useTranslation" "$f" || { echo "MISSING in $f"; exit 1; }; done</automated>
  </verify>
  <done>
    useTranslation is imported and a `const { t } = useTranslation();` (or destructure-equivalent) call exists in each of the five files with translatable rows. ResultViewer's Phase 1 hook is preserved as-is.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract dialog/viewer strings → t() calls + append keys to en.json</name>
  <files>frontend/src/components/GenerationDialog.tsx, frontend/src/components/ResultViewer.tsx, frontend/src/components/SponsorDialog.tsx, frontend/src/components/AiProviderDialog.tsx, frontend/src/components/settings/ProviderSetupModal.tsx, frontend/src/i18n/locales/en.json</files>
  <read_first>
    - STRING-INVENTORY.md tables for each file (don't re-read; use what's already in Task 1's read)
    - STRING-INVENTORY.md Negative list and Phase 2 Consumption Contract sections
  </read_first>
  <action>
    Walk every inventory row for each of the five files (skip ForcedSetupGate — no rows). For each row:

    - If Kind is `do-not-translate`: add a one-line boundary comment immediately above the JSX site, of the form `// i18n: do-not-translate (brand name|USER DATA|technical|...)`. Do not wrap.
    - Otherwise: append the `Key proposal` to `frontend/src/i18n/locales/en.json` with the English value and replace the source-code usage with `t("key", { interp })` or `<Trans i18nKey="...">{children}</Trans>` if there's embedded markup.

    **Vietnamese rewrite rules** (GenerationDialog.tsx specifically — these inventory rows have Vietnamese current strings; the en.json VALUE must be English, not Vietnamese):

    | Line | Vietnamese current string | English value to store under key |
    |------|---------------------------|----------------------------------|
    | 831 | "Bỏ trống để tự sinh motion prompt từ source image ✨" | "Leave blank to auto-generate a motion prompt from the source image ✨" |
    | 833 | "Nhập prompt mồi để feed cho downstream image / video…" | "Enter a seed prompt to feed downstream image / video nodes…" |
    | 835 | "Bỏ trống để tự generate prompt từ upstream nodes ✨" | "Leave blank to auto-generate a prompt from upstream nodes ✨" |
    | 855 | "✨ Đang phân tích image…" | "✨ Analyzing image…" |
    | 857 | "✨ Đang dựng prompt từ upstream context…" | "✨ Building prompt from upstream context…" |
    | 884 | "Quốc gia" | "Country" |
    | 917 | "Mô tả thêm (tuỳ chọn)" | "Additional notes (optional)" |
    | 924 | "Tuổi, kiểu tóc, trang phục, biểu cảm…" | "Age, hair, outfit, expression…" |
    | 1005 | "Chọn ít nhất 1 variant để gen video." | "Select at least 1 variant to generate video." |
    | 1012 | "Sẽ gen <strong>…</strong>" | "Will generate <strong>{{count}}</strong> ..." — use `<Trans>` with `<strong>` placeholder; final English copy: `Will generate <1>{{count}}</1> variant{{plural}}` or similar. Use `<Trans i18nKey="dialog.will_gen" components={{ b: <strong /> }}>` and store the value with `<b>{{count}}</b>` placeholder syntax. The exact wording is at Claude's discretion per CONTEXT.md — pick clear English. |
    | 1203 (tip) | "Sticky — selection được lưu…" | "Sticky — your selection is remembered across dispatches." |
    | 1254 | "Static = locked-off…" | "Static = locked-off camera; cinematic = subtle motion; dynamic = active camera moves." |

    **Brand-adjacent strings** (preserve brand identifiers; only wrap the surrounding label):
    - Line 1169: `"Duration (Omni Flash)"` — value stored under `dialog.omni_duration_label` should be the literal English `"Duration (Omni Flash)"` (brand name embedded in label is fine; we are not extracting "Omni Flash" itself as a key).
    - Line 1227, 1239: brand model names in `<option>` text come from `VIDEO_MODEL_CHIPS` / `IMAGE_MODELS` constants — leave the JSX expression untouched (`{m.label}`) and add a boundary comment.

    **ResultViewer.tsx** rules:
    - The existing Phase 1 `formatRelativeTime` rewrite already uses `t("time.*")` — DO NOT MODIFY the function signature or the time.* keys.
    - The metadata grid (lines 628-657) gets new `result.*` keys per inventory.
    - Line 707 `"Open in Flow ↗"` — "Flow" is a brand name, but the surrounding phrase is translatable. Store value `"Open in Flow ↗"` under `result.open_in_flow`. Brand stays inside the value; not a separate key.
    - Lines 489, 497, 572, 1014, 601: `data.title` — user data, never wrapped, add boundary comments.

    **Store-error sites** (ResultViewer.tsx line 367, GenerationDialog.tsx lines 562, 697): these run in event handlers / async catches. Use `i18n.t("key")` via `import i18n from "../i18n/i18n";` rather than the React hook — same rationale as 02-01 (headless symmetry).

    **`<Trans>` vs `t()` rule** per CONTEXT.md §Claude's Discretion: use `<Trans>` only when the JSX contains embedded markup (e.g., `<strong>`, `<a>`). All other sites use `t()`. This plan likely needs `<Trans>` only for the `dialog.will_gen` row (line 1012).

    After updating each .tsx file, run `cd frontend && npm run lint` to catch typed-key drift. Per-file commits: `feat(02-02-PLAN/GenerationDialog): extract dialog strings`, etc.

    `ForcedSetupGate.tsx` is in `files_modified` only for the grep-gate (the orchestrator may scan it). If the inventory and a fresh grep confirm no translatable strings, leave the file untouched — no need to add useTranslation if it's not used.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run lint` exits 0
    - `grep -cE 't\("(dialog|result|sponsor|provider)\.' frontend/src/components/GenerationDialog.tsx frontend/src/components/ResultViewer.tsx frontend/src/components/SponsorDialog.tsx frontend/src/components/AiProviderDialog.tsx frontend/src/components/settings/ProviderSetupModal.tsx` returns at least 60
    - `grep -cE '"(dialog|result|sponsor|provider)\.' frontend/src/i18n/locales/en.json` returns at least 70 new keys
    - `grep -nE '(Bỏ trống|Quốc gia|Mô tả thêm|Đang phân tích|Đang dựng|Chọn ít nhất|Sẽ gen|Tuổi, kiểu tóc|Sticky — selection được lưu|Static = locked-off)' frontend/src/components/GenerationDialog.tsx` returns 0 — all Vietnamese strings removed
    - `grep -nE '"(Veo|Omni Flash|Nano Banana|Banana Pro|Banana 2)"\s*:' frontend/src/i18n/locales/en.json` returns 0 — brand names did not enter as KEYS (the values may legitimately contain the words "Omni Flash" inside a label phrase, which is allowed)
    - `grep -E 'data\.title' frontend/src/components/ResultViewer.tsx | grep -c 't('` returns 0
    - Existing `time.*` keys in en.json are unchanged
  </acceptance_criteria>
  <done>
    All inventory rows for GenerationDialog, ResultViewer, SponsorDialog, AiProviderDialog, and ProviderSetupModal are extracted. Every Vietnamese string in the dialog is replaced with English. Brand names stay in constants imports; data.title never wrapped. en.json gained ~80 new keys. Per-file commits exist. Lint passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mirror new dialog/viewer keys into tr.json as empty-string stubs</name>
  <files>frontend/src/i18n/locales/tr.json</files>
  <read_first>
    - frontend/src/i18n/locales/en.json (post-Task-2 state)
    - frontend/src/i18n/locales/tr.json (current state — has time.* keys from Phase 1 + node.*/palette.* from 02-01 if merged earlier)
  </read_first>
  <action>
    For every key newly added to en.json in this plan's Task 2 (the dialog.*, result.*, sponsor.*, provider.* keys), append a matching `""` entry to tr.json. Group entries by area prefix; keep one entry per line; valid JSON.

    Do NOT delete or reorder ANY existing tr.json entries (time.* from Phase 1, plus whatever any other 02-NN plan has merged in by the time this runs).

    Parity check at end:
    `node -e "const en=Object.keys(require('./frontend/src/i18n/locales/en.json')); const tr=Object.keys(require('./frontend/src/i18n/locales/tr.json')); const missing=en.filter(k=>!tr.includes(k)); console.log('missing in tr:', missing.length); process.exit(missing.length?1:0)"`

    Commit: `feat(02-02-PLAN/tr-parity): add empty-string stubs for dialog/viewer keys`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; node -e "const en=Object.keys(require('./src/i18n/locales/en.json')); const tr=Object.keys(require('./src/i18n/locales/tr.json')); const missing=en.filter(k=>!tr.includes(k)); console.log('missing in tr:', missing.length); process.exit(missing.length?1:0)"</automated>
  </verify>
  <acceptance_criteria>
    - tr.json has at least the same set of keys as en.json (parity)
    - Newly added tr.json values are all `""`
    - JSON parses
    - `cd frontend && npm run lint` passes
  </acceptance_criteria>
  <done>
    tr.json mirrors en.json at full key parity. All new entries are empty. Commit exists. Lint passes.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run lint` exits 0
2. `cd frontend && npm run build` succeeds
3. `grep -rE '(Bỏ trống|Quốc gia|Mô tả thêm|Đang phân tích|Đang dựng|Chọn ít nhất|Sẽ gen|Tuổi, kiểu tóc)' frontend/src/components` returns 0 — Vietnamese eliminated
4. `grep -nE '"(Veo|Omni Flash|Nano Banana|Banana Pro|Banana 2)"\s*:' frontend/src/i18n/locales/en.json` returns 0 — brand names never used as keys
5. `grep -nE '\bt\((["\x27])[^"\x27]*\1[^)]*\)\s*[+]?\s*data\.title' frontend/src/components/ResultViewer.tsx` returns 0 — data.title not concatenated with t() result
6. tr.json key count >= en.json key count (parity)
</verification>

<success_criteria>
- All ~80 translatable rows from inventory (GenerationDialog 42 + ResultViewer 30 + SponsorDialog 2 + AiProviderDialog 5 + ProviderSetupModal 3) wrapped with `t()` or `<Trans>` (or `i18n.t()` for store-error paths)
- Every Vietnamese string in GenerationDialog replaced with English copy
- ~80 new keys under `dialog.*`, `result.*`, `sponsor.*`, `provider.*` in en.json
- Matching ~80 empty-string entries in tr.json
- Brand identifiers never used as keys (they may appear inside values where they belong contextually)
- `data.title` and user data never wrapped
- `npm run lint` exits 0; `npm run build` succeeds
- Per-file commits per CONTEXT.md §Atomicity rule
</success_criteria>

<output>
Create `.planning/phases/02-english-extraction/02-02-SUMMARY.md` with:
- Counts: rows extracted, do-not-translate skips, Vietnamese rewrites
- Surprises: Vietnamese strings whose English wording diverged from the inventory's hint
- Commits: hashes per file
- Self-check: all must_have artifacts present
</output>
