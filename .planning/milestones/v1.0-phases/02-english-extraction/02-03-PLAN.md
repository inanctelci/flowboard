---
phase: 02-english-extraction
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/SettingsPanel.tsx
  - frontend/src/components/ProjectSidebar.tsx
  - frontend/src/components/ReferencesPanel.tsx
  - frontend/src/components/Toolbar.tsx
  - frontend/src/components/Toaster.tsx
  - frontend/src/components/StatusBar.tsx
  - frontend/src/components/AccountPanel.tsx
  - frontend/src/components/ChatSidebar.tsx
  - frontend/src/components/AiProviderBadge.tsx
  - frontend/src/components/settings/AiProvidersSection.tsx
  - frontend/src/components/settings/ProviderCard.tsx
  - frontend/src/i18n/locales/en.json
  - frontend/src/i18n/locales/tr.json
autonomous: true
requirements:
  - EXTRACT-01
  - EXTRACT-02
  - EXTRACT-06
  - EXTRACT-07
tags:
  - i18n
  - react-i18next
  - panels
  - toolbar
must_haves:
  truths:
    - "All panels (Settings, ProjectSidebar, ReferencesPanel, AccountPanel) render in English from t() calls"
    - "Toolbar renders Flowboard wordmark verbatim (do-not-translate) but Board name aria-labels and tooltips use t()"
    - "StatusBar agent/extension/token indicators use t() except the numeric req/✓/✗ stats which stay raw"
    - "Brand names (Veo, Nano Banana, Flowboard) never appear as keys in en.json"
    - "USER DATA (ref.label, board name) is never wrapped"
    - "npm run lint passes"
  artifacts:
    - path: "frontend/src/components/SettingsPanel.tsx"
      provides: "Model hints + section headings extracted; brand names untouched in constants"
      contains: "useTranslation"
    - path: "frontend/src/components/Toolbar.tsx"
      provides: "Board name aria-label + rename tooltip extracted; Flowboard wordmark preserved verbatim"
      contains: "useTranslation"
    - path: "frontend/src/i18n/locales/en.json"
      provides: "~30 new settings.* + panel.* + sidebar.* + toolbar.* + toaster.* + status.* + account.* + provider.* keys appended"
      contains: "toolbar.board_name"
  key_links:
    - from: "frontend/src/components/SettingsPanel.tsx"
      to: "frontend/src/i18n/locales/en.json"
      via: "t(\"settings.*\") calls"
      pattern: "t\\(\"settings\\."
    - from: "frontend/src/components/Toolbar.tsx"
      to: "frontend/src/constants/"
      via: "Flowboard wordmark imported from constants, not from i18n catalog"
      pattern: "Flowboard"
---

<objective>
Extract translatable strings from the chrome panels and toolbar — many small files, low per-file row counts, but high coverage across the chrome layer. ~30 total translatable rows distributed across 11 files.

Purpose: Satisfy EXTRACT-01, EXTRACT-02, EXTRACT-06, EXTRACT-07 for the panel/toolbar/status surface.

Output:
- All 11 chrome components use `useTranslation()` + the appropriate area prefix (`settings.*`, `panel.*`, `sidebar.*`, `toolbar.*`, `toaster.*`, `status.*`, `account.*`, `chat.*`, `provider.*`)
- en.json gains ~30 new keys
- tr.json gains matching empty-string stubs
- `npm run lint` exits 0
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-english-extraction/02-CONTEXT.md
@.planning/phases/01-infra-audit/STRING-INVENTORY.md

# i18n surface
@frontend/src/i18n/i18n.ts
@frontend/src/i18n/locales/en.json
@frontend/src/i18n/locales/tr.json

# Files to modify (read each ONCE)
@frontend/src/components/SettingsPanel.tsx
@frontend/src/components/ProjectSidebar.tsx
@frontend/src/components/ReferencesPanel.tsx
@frontend/src/components/Toolbar.tsx
@frontend/src/components/Toaster.tsx
@frontend/src/components/StatusBar.tsx
@frontend/src/components/AccountPanel.tsx
@frontend/src/components/ChatSidebar.tsx
@frontend/src/components/AiProviderBadge.tsx
@frontend/src/components/settings/AiProvidersSection.tsx
@frontend/src/components/settings/ProviderCard.tsx
</context>

<append_only_contract>
APPEND-only to en.json and tr.json. Area prefixes: `settings.*`, `panel.*`, `sidebar.*`, `toolbar.*`, `toaster.*`, `status.*`, `account.*`, `chat.*`, `provider.*`. Never delete or reorder existing keys. Every en key has a matching empty tr key.
</append_only_contract>

<tasks>

<task type="auto">
  <name>Task 1: Add useTranslation imports + hooks to all chrome components</name>
  <files>frontend/src/components/SettingsPanel.tsx, frontend/src/components/ProjectSidebar.tsx, frontend/src/components/ReferencesPanel.tsx, frontend/src/components/Toolbar.tsx, frontend/src/components/Toaster.tsx, frontend/src/components/StatusBar.tsx, frontend/src/components/AccountPanel.tsx, frontend/src/components/AiProviderBadge.tsx, frontend/src/components/settings/AiProvidersSection.tsx, frontend/src/components/settings/ProviderCard.tsx</files>
  <read_first>
    - STRING-INVENTORY.md sections for each of these files
    - ChatSidebar.tsx: per inventory, "entire component is commented out in App.tsx; strings deferred to Phase 2 when re-enabled" — verify with a grep before touching. If still commented out / disabled, leave the file alone and document in SUMMARY.
  </read_first>
  <action>
    For each file with translatable rows in the inventory:
    - Add `import { useTranslation } from "react-i18next";`
    - Add `const { t } = useTranslation();` at the top of the primary exported component

    Special cases:
    - **ChatSidebar.tsx**: per inventory it is currently disabled in App.tsx. Run `grep -n 'ChatSidebar' frontend/src/App.tsx` — if the import / render is commented out, leave ChatSidebar.tsx UNTOUCHED in this plan and note in SUMMARY ("ChatSidebar.tsx deferred — component disabled in App.tsx as of 2026-06-10; revisit when re-enabled in v1.3+"). If somehow now active, treat normally: add useTranslation + the strings the file actually contains.
    - **Toolbar.tsx**: line 39 `"Flowboard"` is the brand wordmark — do-not-translate. Line 57 `boardName || "Untitled"` — "Untitled" is a USER DATA default (TYPE_TITLE defaults are also do-not-translate per CONTEXT.md §Ambiguity #3). Add boundary comments at these sites.
    - **StatusBar.tsx**: line 62 `"req ${reqCount} · ✓${okCount} · ✗${failCount}"` is a numeric stats line — do-not-translate. The labels in lines 46-56 are translatable (status.agent_*, status.ext_*, status.token_age).

    No dynamic key construction. No `t()` calls in this task — that's Task 2.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; for f in src/components/SettingsPanel.tsx src/components/ProjectSidebar.tsx src/components/ReferencesPanel.tsx src/components/Toolbar.tsx src/components/Toaster.tsx src/components/StatusBar.tsx src/components/AccountPanel.tsx src/components/AiProviderBadge.tsx src/components/settings/AiProvidersSection.tsx src/components/settings/ProviderCard.tsx; do grep -q "useTranslation" "$f" || { echo "MISSING in $f"; exit 1; }; done</automated>
  </verify>
  <done>
    useTranslation import + hook call present in each of the 10 active chrome files. ChatSidebar.tsx untouched if disabled; documented in SUMMARY otherwise.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract chrome strings → t() calls + append keys to en.json</name>
  <files>frontend/src/components/SettingsPanel.tsx, frontend/src/components/ProjectSidebar.tsx, frontend/src/components/ReferencesPanel.tsx, frontend/src/components/Toolbar.tsx, frontend/src/components/Toaster.tsx, frontend/src/components/StatusBar.tsx, frontend/src/components/AccountPanel.tsx, frontend/src/components/AiProviderBadge.tsx, frontend/src/components/settings/AiProvidersSection.tsx, frontend/src/components/settings/ProviderCard.tsx, frontend/src/i18n/locales/en.json</files>
  <read_first>
    - STRING-INVENTORY.md tables for each chrome file (already in context from Task 1)
  </read_first>
  <action>
    Walk every inventory row for each chrome file. For each row:

    - If Kind is `do-not-translate`: add a one-line boundary comment above the JSX site. The do-not-translate items in this plan's scope are:
      - Toolbar.tsx line 39 (`"Flowboard"` brand wordmark)
      - Toolbar.tsx line 57 (`boardName || "Untitled"` — board name is USER DATA; "Untitled" fallback stays untranslated)
      - SettingsPanel.tsx lines 29, 32, 54, 58, 62, 68 (brand model identifiers — Nano Banana Pro, Nano Banana 2, Veo 3.1 Lite/Fast/Quality/Lite (Low Priority))
      - SettingsPanel.tsx line 33 ("NARWHAL — faster, lighter checkpoint" — wait, the inventory marks this as a TRANSLATABLE hint key `settings.nano_banana_2_hint` but flags "NARWHAL" as an internal code name. The VALUE stored under the key keeps "NARWHAL" inside the English string; the key itself is translatable. So: extract the full hint as `t("settings.nano_banana_2_hint")` with the value containing "NARWHAL" verbatim. Same pattern for `settings.nano_banana_pro_hint` containing "GEM_PIX_2".)
      - StatusBar.tsx line 62 (numeric req/ok/fail stats)
      - ReferencesPanel.tsx `ref.label` references (USER DATA)
    - Otherwise: append the `Key proposal` to en.json and wrap the source site.

    **Special interpolation cases**:
    - ProjectSidebar.tsx line 72: `"Pushed ${ok} board${...} to Flow ✓"` — pluralized; use `_one`/`_other` suffix keys: `sidebar.boards_pushed_one`: "Pushed {{count}} board to Flow ✓", `sidebar.boards_pushed_other`: "Pushed {{count}} boards to Flow ✓". Call with `t("sidebar.boards_pushed", { count: ok })`.
    - ProjectSidebar.tsx line 74: `"Pushed ${ok}, ${fail} failed — see agent log"` — two-variable interpolation: `sidebar.boards_pushed_partial`: "Pushed {{ok}}, {{fail}} failed — see agent log". Call with `t("sidebar.boards_pushed_partial", { ok, fail })`.
    - ProjectSidebar.tsx delete confirm interpolation: `"Are you sure you want to delete \"${name}\"?"` — name is USER DATA; the surrounding sentence is translatable. Key: `sidebar.delete_confirm`: `"Are you sure you want to delete \"{{name}}\"?"`. Call with `t("sidebar.delete_confirm", { name })` — note the `{name}` interpolates the user's board name verbatim without going through t() lookup.
    - ReferencesPanel.tsx line 89: `"No references match \"${query}\"."` — same pattern; query is USER DATA, sentence is translatable.
    - StatusBar.tsx line 56: `"token ${tokenAge}"` — key: `status.token_age`: "token {{age}}". Call with `t("status.token_age", { age: tokenAge })`.

    **Store-error sites** (ProjectSidebar.tsx ~line 80 "sync failed"): use `i18n.t("sidebar.sync_failed")` via `import i18n from "../i18n/i18n";` for symmetry with other 02-NN plans.

    **Per CONTEXT.md §Claude's Discretion — deduplication**: If multiple chrome components share an identical string (e.g., "Save", "Cancel", "Close", "Apply"), the executor may EITHER:
    1. Use per-file-prefix keys (`settings.save`, `panel.delete`, `provider.apply`, etc. — what the inventory proposes), OR
    2. Introduce a `common.*` prefix for truly shared chrome strings.

    Choose option 1 (per-file prefix) as the default — it's what the inventory specifies and avoids cross-plan coordination on a shared `common.*` namespace. The duplication cost is small (~20 bytes per duplicate value); the merge-conflict avoidance is large.

    After each file's edits, run `cd frontend && npm run lint`. Per-file commits: `feat(02-03-PLAN/<file>): extract strings to i18n`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run lint` exits 0
    - `grep -cE 't\("(settings|panel|sidebar|toolbar|toaster|status|account|provider)\.' frontend/src/components/` (recursively) returns at least 25
    - `grep -cE '"(settings|panel|sidebar|toolbar|toaster|status|account|chat|provider)\.' frontend/src/i18n/locales/en.json` returns at least 25 new keys
    - `grep -nE '"(Flowboard|Veo|Nano Banana|Omni Flash)"\s*:' frontend/src/i18n/locales/en.json` returns 0 (brand names not used as keys)
    - Toolbar.tsx still renders the literal `Flowboard` wordmark (brand preservation):
      `grep -E ">Flowboard<" frontend/src/components/Toolbar.tsx` returns 1+ hit OR `Flowboard` is preserved as a constant import
    - `grep -E 'ref\.label' frontend/src/components/ReferencesPanel.tsx | grep -c 't('` returns 0 — user-data not wrapped
  </acceptance_criteria>
  <done>
    All chrome inventory rows are extracted or marked do-not-translate. en.json gained ~30 new keys across the area prefixes. ChatSidebar.tsx confirmed disabled or extracted. Per-file commits exist. Lint passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mirror new chrome keys into tr.json as empty-string stubs</name>
  <files>frontend/src/i18n/locales/tr.json</files>
  <read_first>
    - frontend/src/i18n/locales/en.json (post-Task-2 state)
    - frontend/src/i18n/locales/tr.json
  </read_first>
  <action>
    For every key newly added to en.json by this plan, append a matching `""` entry to tr.json. Group by area prefix; valid JSON.

    Parity check:
    `node -e "const en=Object.keys(require('./frontend/src/i18n/locales/en.json')); const tr=Object.keys(require('./frontend/src/i18n/locales/tr.json')); const missing=en.filter(k=>!tr.includes(k)); console.log('missing in tr:', missing.length); process.exit(missing.length?1:0)"`

    Commit: `feat(02-03-PLAN/tr-parity): add empty-string stubs for chrome keys`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; node -e "const en=Object.keys(require('./src/i18n/locales/en.json')); const tr=Object.keys(require('./src/i18n/locales/tr.json')); const missing=en.filter(k=>!tr.includes(k)); console.log('missing in tr:', missing.length); process.exit(missing.length?1:0)"</automated>
  </verify>
  <acceptance_criteria>
    - tr.json parity with en.json
    - New entries all `""`
    - JSON valid
    - Lint passes
  </acceptance_criteria>
  <done>
    tr.json mirrors en.json. Commit exists. Lint passes.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run lint` exits 0
2. `cd frontend && npm run build` succeeds
3. `grep -nE '"(Flowboard|Veo 3.1|Nano Banana|Omni Flash)"\s*:' frontend/src/i18n/locales/en.json` returns 0 — brand identifiers never used as keys
4. Toolbar still ships the literal `Flowboard` wordmark
5. tr.json parity: same key set as en.json
</verification>

<success_criteria>
- All ~30 inventory rows across the 10-11 chrome files extracted or marked do-not-translate
- Brand names (Flowboard, Veo 3.1, Nano Banana, Omni Flash) preserved in their components or constants, never used as i18n keys
- User-data refs (ref.label, board name) never wrapped
- ~30 new keys in en.json; matching empty stubs in tr.json
- `npm run lint` exits 0
- ChatSidebar.tsx status confirmed (touched only if active, otherwise documented as deferred)
- Per-file commits
</success_criteria>

<output>
Create `.planning/phases/02-english-extraction/02-03-SUMMARY.md` with:
- Counts: rows extracted, do-not-translate skips, deduplication decisions
- ChatSidebar.tsx status (touched / deferred)
- Surprises
- Commit hashes
- Self-check
</output>
