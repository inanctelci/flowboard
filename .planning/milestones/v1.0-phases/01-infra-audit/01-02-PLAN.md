---
phase: 01-infra-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-infra-audit/STRING-INVENTORY.md
autonomous: true
requirements:
  - INFRA-07
tags:
  - i18n
  - audit
  - inventory
  - extraction

must_haves:
  truths:
    - "`.planning/phases/01-infra-audit/STRING-INVENTORY.md` exists at the spec'd path."
    - "Running `wc -l .planning/phases/01-infra-audit/STRING-INVENTORY.md` reports more than 100 lines — the audit is non-trivial (the 'invisible 40%' alone has dozens of sites)."
    - "The inventory contains a per-file section for every `.tsx` file under `frontend/src/canvas/`, `frontend/src/components/`, and the top level of `frontend/src/` (App.tsx)."
    - "The inventory contains a per-file section for every `.ts` file under `frontend/src/store/`, `frontend/src/api/`, `frontend/src/lib/`, and `frontend/src/components/activity/`."
    - "Each file section uses the markdown table schema from RESEARCH.md §6: columns `Line(s) | Current string | Key proposal | Kind | Notes`."
    - "The `Kind` column uses values from the closed enum: `text-node`, `aria-label`, `title-attr`, `placeholder`, `store-error`, `utility-fn`, `toaster-msg`, `activity-label`, `error-humanizer`, `do-not-translate`."
    - "Every product/model brand identifier (Veo 3.1 Lite, Nano Banana Pro, Nano Banana 2, Omni Flash, Flowboard, etc.) appears with `Kind = do-not-translate` and a `Notes` cell calling out the negative-list rule (per D-09 in research: product names stay in `frontend/src/constants/`)."
    - "Every user-authored data path (`data.title`, `data.prompt`, `data.aiBrief`, `ref.label`, `board.name`) appears with `Kind = do-not-translate` and `Notes` flagging it as USER DATA — never wrap with t()."
    - "Minimum required coverage (verifiable by grep against the inventory file): `NodeCard.tsx` aria-labels at lines 170/189/192, `api/client.ts` all 4 humanizeBackendError branch strings, `store/generation.ts` error string near line 170, `components/activity/activity-meta.ts` ACTIVITY_TYPE_META labels, `Toaster.tsx` aria-label at line 58, `constants/character.ts` enum entries marked do-not-translate."
    - "The inventory includes a top-level header section explaining the schema, the Kind enum, and the 'do-not-translate' negative list — Phase 2 executors must be able to read this file in isolation without rereading RESEARCH.md."
    - "The inventory includes a final 'Phase 2 Consumption Contract' section reproducing the 4-step extraction recipe from RESEARCH.md §6 so Phase 2 plans can lift it verbatim."
  artifacts:
    - path: ".planning/phases/01-infra-audit/STRING-INVENTORY.md"
      provides: "Per-file inventory of every user-visible string site in frontend/src/, classified by Kind, with proposed i18n keys for translatable sites and do-not-translate flags for product names and user-authored data"
      min_lines: 100
      contains: "STRING-INVENTORY"
  key_links:
    - from: ".planning/phases/01-infra-audit/STRING-INVENTORY.md"
      to: "frontend/src/canvas/NodeCard.tsx"
      via: "explicit line-numbered references for aria-labels at 170, 189, 192"
      pattern: "NodeCard.tsx"
    - from: ".planning/phases/01-infra-audit/STRING-INVENTORY.md"
      to: "frontend/src/api/client.ts"
      via: "humanizeBackendError branch strings catalogued by branch"
      pattern: "humanizeBackendError"
    - from: ".planning/phases/01-infra-audit/STRING-INVENTORY.md"
      to: "frontend/src/components/activity/activity-meta.ts"
      via: "ACTIVITY_TYPE_META labels enumerated"
      pattern: "activity-meta"
    - from: ".planning/phases/01-infra-audit/STRING-INVENTORY.md"
      to: "frontend/src/constants/character.ts"
      via: "do-not-translate marker for CHARACTER_COUNTRIES, CHARACTER_VIBES, CHARACTER_GENDERS"
      pattern: "do-not-translate"
---

<objective>
Produce `.planning/phases/01-infra-audit/STRING-INVENTORY.md` — the per-file enumeration of every user-visible string site in `frontend/src/`, classified by the closed `Kind` enum from RESEARCH.md §6, with proposed i18n keys for translatable sites and explicit `do-not-translate` flags for product/model brand identifiers and user-authored data. This document IS the Phase 2 extraction checklist. Without it, Phase 2's blast radius is unbounded ("extract everything"); with it, Phase 2 becomes a deterministic file-by-file walk ("extract this concrete list").

Purpose: De-risk Phase 2 by enumerating the "invisible 40%" — aria-labels, store error strings, utility function strings, activity-meta labels, error-humanizer branches — that a blanket JSX text-node grep would miss. Establish the user-data boundary (NodeCard.tsx and ReferencesPanel.tsx) with explicit markers so a future executor cannot accidentally wrap `data.title` or `ref.label` in `t()`. Lock the negative list (Veo 3.1 Lite, Nano Banana Pro, Omni Flash, Flowboard) so product names never enter the locale catalogs (per D-08 in research).

Output: A single markdown document at `.planning/phases/01-infra-audit/STRING-INVENTORY.md` containing per-file tables with the RESEARCH.md §6 schema, ready to be consumed verbatim by Phase 2 PLAN files.

This plan runs in parallel with `01-01-PLAN.md` (no `files_modified` overlap — Plan 01 modifies `frontend/src/` files, Plan 02 only writes a planning document). It does not depend on the i18n scaffold being installed first; the inventory is a pure read/audit pass against the existing codebase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-infra-audit/01-CONTEXT.md
@.planning/phases/01-infra-audit/01-RESEARCH.md
@.planning/phases/01-infra-audit/01-PATTERNS.md
@.planning/research/SUMMARY.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
The following file is NEW in Phase 1 — drift-guard MUST skip it during source grounding:

- `.planning/phases/01-infra-audit/STRING-INVENTORY.md` — Phase 2 extraction checklist (per-file table of string sites by Kind)

This document is a planning artifact, not source code. It does not affect `frontend/` and does not require lint/build verification.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate JSX string sites in canvas/ and components/ (text-nodes + attribute props)</name>
  <files>
    .planning/phases/01-infra-audit/STRING-INVENTORY.md
  </files>
  <read_first>
    .planning/phases/01-infra-audit/01-RESEARCH.md (§6 String Inventory Schema; §7 Verification Checklist item V7 — minimum required coverage),
    .planning/codebase/STRUCTURE.md (frontend/src directory layout),
    frontend/src/canvas/ (every .tsx file — full read of NodeCard.tsx, Board.tsx, VariantEdge.tsx, AddNodePalette.tsx, and any other .tsx in this directory),
    frontend/src/components/ (every .tsx file — full read of GenerationDialog.tsx, ResultViewer.tsx, Toaster.tsx, Toolbar.tsx, StatusBar.tsx, AccountPanel.tsx, AiProviderBadge.tsx, AiProviderDialog.tsx, ChatSidebar.tsx, ForcedSetupGate.tsx, ProjectSidebar.tsx, ReferencesPanel.tsx, SettingsPanel.tsx, SponsorDialog.tsx, and any settings/ or activity/ subdirectory .tsx files),
    frontend/src/App.tsx
  </read_first>
  <action>
    Create `.planning/phases/01-infra-audit/STRING-INVENTORY.md` with the header block first (see schema below), THEN walk every `.tsx` file under `frontend/src/canvas/`, `frontend/src/components/` (including nested directories like `components/activity/`, `components/settings/`), and `frontend/src/App.tsx`.

    For each file, emit a markdown section using this structure:

    ```
    ## frontend/src/canvas/NodeCard.tsx

    | Line(s) | Current string | Key proposal | Kind | Notes |
    |---------|----------------|--------------|------|-------|
    | 65 | "Upload" | `node.upload` | text-node | Button label |
    | 170 | "Replace character image" | `node.replace_char_image` | aria-label | aria-label attr |
    | 189 | "Save this character..." | `node.save_to_library_title` | title-attr | tooltip |
    | 192 | "Save to library" | `node.save_to_library` | aria-label | button aria-label |
    | 221 | data.title | — | do-not-translate | USER DATA — node title typed by user; never wrap in t() |
    ```

    **Header block** (top of the file, before any per-file sections):

    ```
    # Phase 1 String Inventory — Flowboard frontend/src/

    **Generated:** 2026-06-10
    **Source:** Phase 1 audit pass over frontend/src/ (canvas, components, store, api, lib, App.tsx)
    **Consumer:** Phase 2 (EXTRACT-01..07) extraction plans

    ## How to read this file

    Each section below is one source file. Each row is one string site identified by line number, current verbatim string (truncated to ~60 chars with `…`), a proposed i18n key in `area.concept` dot-notation, and a `Kind` from the closed enum.

    ## Kind enum

    | Kind | Meaning |
    |------|---------|
    | `text-node` | JSX text node: `<span>Text</span>` |
    | `aria-label` | `aria-label="..."` JSX prop |
    | `title-attr` | `title="..."` JSX prop (tooltip) |
    | `placeholder` | `placeholder="..."` JSX prop |
    | `store-error` | String set on a Zustand store `error` slot |
    | `utility-fn` | String returned by a pure `.ts` function |
    | `toaster-msg` | String that flows to the Toaster component |
    | `activity-label` | String in `activity-meta.ts` label/status tables |
    | `error-humanizer` | Branch return value in `humanizeBackendError` |
    | `do-not-translate` | Product name, model name, brand identifier, or user-authored data — never enters catalog |

    ## Negative list (do-not-translate)

    These appear in JSX alongside translatable strings and MUST NOT be wrapped in `t()`:

    - **Product/model brand identifiers** (per D-08 — research SUMMARY.md §4): `Veo 3.1 Lite`, `Veo 3.1 Fast`, `Veo 3.1 Quality`, `Veo 3.1 Lite Relaxed`, `Omni Flash`, `Omni Flash · 4s/6s/8s/10s`, `Nano Banana Pro`, `Nano Banana 2`, `Banana Pro`, `Banana 2`, `Flowboard` (product name in `<title>` and error messages).
    - **User-authored data fields**: `data.title`, `data.prompt`, `data.aiBrief`, `ref.label`, `board.name`, chat message bodies. These render verbatim via React's JSX escaping — wrapping them in `t()` would attempt translation against a key that is actually a user phrase.
    - **`frontend/src/constants/character.ts` enum entries**: `CHARACTER_COUNTRIES`, `CHARACTER_VIBES`, `CHARACTER_GENDERS` — these are data enum values passed to the backend as API parameters, NOT UI display strings.

    ## Phase 2 Consumption Contract

    Phase 2 executes file-by-file in order of String Density (files with most rows first). For each row:
    1. If `Kind = do-not-translate` — add a code comment marking the boundary; skip extraction.
    2. Otherwise — add `Key proposal` to `en.json`, wrap the string with `t("key.proposal")` or `aria-label={t("key.proposal")}` etc., appropriate for the Kind.
    3. `const { t } = useTranslation()` goes at the top of any React component touched.
    4. For `store-error`, `utility-fn`, `error-humanizer` — use `import i18n from "../i18n/i18n"; i18n.t("key")` (the headless singleton — `useTranslation()` is a hooks violation in `.ts` files).

    ---

    ```

    **Auditing methodology for each .tsx file:**

    1. Read the file end to end.
    2. Identify every JSX text node containing user-visible English copy: `<span>Generate</span>`, `<button>Save</button>`, `<h2>Settings</h2>`, etc. Record each as `Kind = text-node`.
    3. Identify every JSX attribute prop carrying user-visible English copy: `aria-label="..."`, `title="..."`, `placeholder="..."`, `alt="..."`. Record by attribute kind (`aria-label`, `title-attr`, `placeholder`).
    4. Identify dynamic JSX expressions that render user-authored data (`{data.title}`, `{ref.label}`, `{node.data.prompt}`, `{message.body}`, etc.). Record with `Kind = do-not-translate` and `Notes = USER DATA — never wrap`. Include the exact line number.
    5. Identify constants imported and rendered as JSX (`{IMAGE_MODEL_LABELS[node.data.imageModel]}`, `{VIDEO_QUALITY_LABELS[...]}` , etc.). Record with `Kind = do-not-translate` and `Notes = brand identifier — stays in constants/`.
    6. Propose an i18n key in `area.concept` dot-notation for each translatable row. `area` ≈ the file's role (`node`, `dialog`, `result`, `toolbar`, `panel`, `settings`, `chat`, `toaster`). `concept` ≈ what the string does (`upload`, `save_to_library`, `cancel`, `confirm`, `generating`, `loading_board`). Keys can be revised in Phase 2 — the proposal exists to seed extraction without naming friction.
    7. For strings longer than ~60 characters, truncate the `Current string` cell to the first 50-55 chars + `…` so the table stays readable. Phase 2 reads the full string from source.

    **Files this task covers (do NOT include `frontend/src/store/`, `frontend/src/api/`, `frontend/src/lib/`, or `frontend/src/components/activity/*.ts` — those are Task 2's scope):**

    - `frontend/src/App.tsx`
    - Every `*.tsx` file in `frontend/src/canvas/` (recursive — at minimum: `Board.tsx`, `NodeCard.tsx`, `VariantEdge.tsx`, `AddNodePalette.tsx`; check for others)
    - Every `*.tsx` file in `frontend/src/components/` (recursive into `components/activity/`, `components/settings/`)

    **Minimum required coverage** (verifiable post-write per RESEARCH.md §7 item V7):
    - `frontend/src/canvas/NodeCard.tsx` lines 170, 189, 192 each have a row in their file section.
    - `frontend/src/components/Toaster.tsx` line 58 has a row.
    - Every brand identifier in the negative list above appears as at least one `do-not-translate` row in some file section (the constants-loaded labels render in `ResultViewer.tsx` and other components — record the render sites, not the constants file declarations).
    - `data.title`, `data.prompt`, `data.aiBrief`, `ref.label`, `board.name` each appear as `do-not-translate` rows in their rendering files.

    Reading guidance: read each file completely once and extract every site in a single pass — do NOT re-read for "one more thing." Use Grep with specific patterns (e.g. `grep -nE 'aria-label="[A-Z][^"]+"' frontend/src/canvas/NodeCard.tsx`) to confirm line numbers when needed.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q '## frontend/src/canvas/NodeCard.tsx' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q '## frontend/src/components/Toaster.tsx' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q '## frontend/src/components/ResultViewer.tsx' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q '## frontend/src/App.tsx' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q 'do-not-translate' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q 'USER DATA' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q 'Veo 3.1' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q 'Nano Banana' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -c '^|' .planning/phases/01-infra-audit/STRING-INVENTORY.md | awk '{exit ($1 &lt; 30)}'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-infra-audit/STRING-INVENTORY.md` exists and includes a header block with schema, Kind enum, negative list, and Phase 2 Consumption Contract.
    - The file contains a section (`## frontend/src/...`) for every `.tsx` file under `frontend/src/canvas/` and `frontend/src/components/` plus `frontend/src/App.tsx`.
    - At least one section uses the schema columns `Line(s) | Current string | Key proposal | Kind | Notes`.
    - The file contains at least 30 table rows (`^|` pipe-prefix counts) across all per-file sections — this is a coarse non-trivial-content check.
    - `Veo 3.1`, `Nano Banana`, `do-not-translate`, and `USER DATA` all appear as substrings.
    - NodeCard.tsx section explicitly references lines 170, 189, 192 with `aria-label` Kind on each.
    - Toaster.tsx section references line 58 with `aria-label` Kind.
  </acceptance_criteria>
  <done>
    JSX-side inventory of canvas/ + components/ + App.tsx complete. Non-JSX sites (stores, api, lib, activity-meta) come in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append non-JSX string sites — stores, api/client.ts, lib/, activity-meta, error-humanizer branches — and finalize document</name>
  <files>
    .planning/phases/01-infra-audit/STRING-INVENTORY.md
  </files>
  <read_first>
    .planning/phases/01-infra-audit/STRING-INVENTORY.md (the file produced by Task 1 — read to confirm the header block and per-file table schema before appending),
    .planning/phases/01-infra-audit/01-RESEARCH.md (§6 schema and §7 minimum coverage list),
    frontend/src/store/ (every .ts file — board.ts, chat.ts, generation.ts, pipeline.ts, references.ts, settings.ts; look for `error:` slot assignments and any string passed to setters),
    frontend/src/api/client.ts (humanizeBackendError function — lines 18-56 — and any other user-visible string return),
    frontend/src/lib/ (any .ts file producing user-visible strings — e.g. storyboardPrompt.ts, autoBrief.ts, github.ts),
    frontend/src/components/activity/activity-meta.ts (ACTIVITY_TYPE_META labels and status strings),
    frontend/src/components/activity/useActivityFeed.ts (any string returns or constants),
    frontend/src/constants/character.ts (full file — to enumerate the do-not-translate entries by name)
  </read_first>
  <action>
    Append new per-file sections to `.planning/phases/01-infra-audit/STRING-INVENTORY.md` (created by Task 1). Do not rewrite the file — read it, then append. The Kind enum, header, and per-file schema are already established; Task 2 only adds new file sections.

    **Files this task covers (Task 1 already covered the JSX side):**

    1. **Zustand stores** — `frontend/src/store/board.ts`, `chat.ts`, `generation.ts`, `pipeline.ts`, `references.ts`, `settings.ts`. For each store, find every assignment to an `error: ...` slot, every literal string passed to a setter that surfaces to the UI, and any user-visible message inside an action body. Kind values used:
       - `store-error` — string written to a store's `error: string | null` slot
       - `toaster-msg` — string that explicitly flows through the Toaster component (the four stores `chat / pipeline / generation / board` all feed Toaster.tsx)
       - `do-not-translate` — error tokens that are backend-supplied and pass through verbatim (e.g. `paygate_tier_unknown` literal in store code that comes from the backend)
       - Minimum: `frontend/src/store/generation.ts` has a known store error string at approximately line 170 (per RESEARCH.md §7 V7) — record it explicitly.

    2. **API client** — `frontend/src/api/client.ts`. Catalog all four `humanizeBackendError` branch return strings (lines 21-27 paygate_tier_unknown, lines 30-36 no_media_id_in_upload_response, lines 39-43 captcha_failed: no current window, line 53 `Flow rejected: ...`). Each gets `Kind = error-humanizer` and a proposed key under `error.*` (e.g. `error.paygate_tier_unknown`, `error.upload_no_media_id`, `error.captcha_no_window`, `error.flow_rejected_prefix`). The `Flow rejected: ` prefix is the translatable piece; the dynamic suffix (e.g. `unsafe generation`) is the backend token and stays English.

    3. **Lib helpers** — `frontend/src/lib/`. Read every `.ts` file in this directory and record any function that returns a user-visible English string (e.g. prompt-composition helpers in `storyboardPrompt.ts`, brief-composition helpers in `autoBrief.ts`). Kind = `utility-fn`. If a file contains only LLM-prompt template strings (never directly shown to the user; consumed by the agent), record with `Kind = do-not-translate` and Notes explaining that LLM prompt strings are not UI copy (per PROJECT.md §Out of Scope — "LLM-generated content").

    4. **Activity meta** — `frontend/src/components/activity/activity-meta.ts`. Read the entire file and enumerate every entry in the `ACTIVITY_TYPE_META` table (or equivalently named export). Each `label` field that is a user-visible string gets a row with `Kind = activity-label`. Status enum values (`running`, `done`, `queued`, etc.) that surface in the UI get rows with `Kind = activity-label`; internal-only fields (icon names, color codes) get `Kind = do-not-translate`. Key proposals use the `activity.*` area prefix.

    5. **Constants — character.ts** — `frontend/src/constants/character.ts`. Read the full file. Enumerate each entry in `CHARACTER_COUNTRIES`, `CHARACTER_VIBES`, `CHARACTER_GENDERS` as a single grouped row per export (do not list every country individually unless the file is short; a single representative row with `Notes = "All 50+ entries are API parameter values, NOT UI display — Kind = do-not-translate for the entire constant"` is acceptable for compactness). Per D-08 / SUMMARY.md §4: these are data enum values passed to the backend, never translated.

    6. **Final density summary section** — append at the end of the document, after all per-file sections:

       ```
       ## String Density Summary

       Files with the highest extraction load for Phase 2 (rows count = translatable rows, excluding do-not-translate):

       | File | Translatable rows | do-not-translate rows | Notes |
       |------|-------------------|----------------------|-------|
       | frontend/src/canvas/NodeCard.tsx | N | M | Highest density + user-data risk |
       | frontend/src/components/GenerationDialog.tsx | N | M | Dialog form fields |
       | frontend/src/components/ResultViewer.tsx | N | M | Metadata grid + buttons |
       | ... | | | |

       Total translatable rows: N
       Total do-not-translate rows: M
       ```

       Counts come from the actual rows the executor recorded. Sort by translatable rows descending.

    **Approach guidance:**
    - Read each file once and extract every site in one pass.
    - For each store, focus on the strings that surface to the UI. Internal `console.warn` strings ARE NOT UI strings — exclude them (they are engineer-only per CONVENTIONS.md §Logging §Frontend).
    - For activity-meta.ts, the icon character (e.g. `"◎"`, `"▣"`, `"▶"`) is a glyph rendered as text — record as `do-not-translate` with Notes "Unicode glyph, not localizable copy".
    - Use grep with specific patterns to verify completeness when finalizing the density summary, e.g. `grep -rn 'set({ error:' frontend/src/store/` to confirm all store-error sites are catalogued.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q '## frontend/src/store/generation.ts' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q '## frontend/src/api/client.ts' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q '## frontend/src/components/activity/activity-meta.ts' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q '## frontend/src/constants/character.ts' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q 'error-humanizer' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q 'activity-label' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q 'store-error' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; grep -q 'String Density Summary' .planning/phases/01-infra-audit/STRING-INVENTORY.md &amp;&amp; wc -l .planning/phases/01-infra-audit/STRING-INVENTORY.md | awk '{exit ($1 &lt; 100)}'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-infra-audit/STRING-INVENTORY.md` now contains sections for every `.ts` file under `frontend/src/store/`, plus `frontend/src/api/client.ts`, every relevant `frontend/src/lib/*.ts`, `frontend/src/components/activity/activity-meta.ts`, and `frontend/src/constants/character.ts`.
    - All four `humanizeBackendError` branches are catalogued under `Kind = error-humanizer` in the `frontend/src/api/client.ts` section.
    - `frontend/src/store/generation.ts` has a row near line 170 for its store-error string.
    - At least one `Kind = activity-label` row exists in the `activity-meta.ts` section.
    - At least one `Kind = store-error` row exists.
    - The `String Density Summary` table exists at the end of the document and ranks files by translatable row count.
    - Total file line count exceeds 100.
  </acceptance_criteria>
  <done>
    STRING-INVENTORY.md is complete: per-file sections for all .tsx and .ts files under frontend/src/, header block + Kind enum + negative list + Phase 2 Consumption Contract + String Density Summary. Phase 2 can execute against this document without re-grepping the codebase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no runtime boundaries) | This plan produces ONLY a planning document. It modifies no source code, installs no dependencies, runs no servers, and ships no artifacts to the production bundle. No runtime trust boundary exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Information Disclosure | `STRING-INVENTORY.md` is committed to the repo (public on GitHub per PROJECT.md) | accept | The document enumerates UI string sites in a public open-source repo. The source code itself is already public; the inventory adds no new exposure. No PII, no secrets, no credentials are catalogued — only translatable copy and brand identifiers. |
| T-01-02-02 | Tampering | A future contributor edits STRING-INVENTORY.md before Phase 2 reads it | accept | The document is reviewed by the Phase 2 planner before extraction begins; the Phase 2 PLAN frontmatter references the inventory and lint/build cannot regress against a doc-only file. Phase 2 source grounding cross-references PATTERNS.md and RESEARCH.md, which are the canonical specs. The inventory is a working checklist, not a contract. |
| T-01-02-SC | Tampering | npm package installs | n/a (out of scope) | This plan installs no packages. Package legitimacy is handled by Plan 01-01 Task 1. |
</threat_model>

<verification>
**Plan-level checks:**

1. `.planning/phases/01-infra-audit/STRING-INVENTORY.md` exists.
2. `wc -l .planning/phases/01-infra-audit/STRING-INVENTORY.md` reports more than 100 lines.
3. Per-file section headers (`## frontend/src/...`) cover at minimum the 6 files called out in RESEARCH.md §7 V7:
   - `frontend/src/canvas/NodeCard.tsx`
   - `frontend/src/api/client.ts`
   - `frontend/src/store/generation.ts`
   - `frontend/src/components/activity/activity-meta.ts`
   - `frontend/src/components/Toaster.tsx`
   - `frontend/src/constants/character.ts`
4. The closed `Kind` enum (10 values) is documented in the header block and used in at least 3 distinct categories across the per-file sections (`text-node`, `aria-label`, `store-error`, `error-humanizer`, `activity-label`, `do-not-translate` should all appear at minimum).
5. The negative list is explicit and includes all brand identifiers (Veo 3.1 *, Nano Banana *, Omni Flash, Flowboard, character.ts entries).
6. User-authored data fields (`data.title`, `data.prompt`, `data.aiBrief`, `ref.label`, `board.name`) are flagged `do-not-translate` with `USER DATA` notes.
7. A `String Density Summary` section ranks files by translatable row count.

This plan does NOT require lint or build verification — it writes only to `.planning/`. It also does NOT require a human-check, since the deliverable is a planning artifact reviewed by the Phase 2 planner rather than a runtime change.
</verification>

<success_criteria>
- `.planning/phases/01-infra-audit/STRING-INVENTORY.md` exists, exceeds 100 lines, and uses the RESEARCH.md §6 schema (5-column table per file).
- Every `.tsx` file under `frontend/src/canvas/`, `frontend/src/components/`, plus `frontend/src/App.tsx` has a per-file section.
- Every `.ts` file under `frontend/src/store/` plus `frontend/src/api/client.ts`, relevant `frontend/src/lib/*.ts`, `frontend/src/components/activity/activity-meta.ts`, and `frontend/src/constants/character.ts` has a per-file section.
- Minimum coverage from RESEARCH.md §7 V7 is met:
  - NodeCard.tsx aria-labels at 170/189/192 are explicit rows.
  - api/client.ts has rows for all 4 humanizeBackendError branches with `Kind = error-humanizer`.
  - store/generation.ts has a row near line 170 with `Kind = store-error`.
  - activity-meta.ts has rows for ACTIVITY_TYPE_META labels with `Kind = activity-label`.
  - Toaster.tsx has a row at line 58 with `Kind = aria-label`.
  - constants/character.ts has rows marked `do-not-translate` for CHARACTER_COUNTRIES / CHARACTER_VIBES / CHARACTER_GENDERS.
- The header block contains the Kind enum, the negative list (product names + user-authored data + character constants), and the Phase 2 Consumption Contract.
- A `String Density Summary` table at the end of the file ranks per-file translatable row counts.
- INFRA-07 (catalog files at `frontend/src/i18n/locales/en.json` and `tr.json`) is supported by this plan in the sense that the inventory's negative list defines what MUST NOT appear in those catalog files in Phase 2 — Plan 01-01 creates the catalog files; Plan 01-02 defines their boundaries.
</success_criteria>

<output>
Create `.planning/phases/01-infra-audit/01-02-SUMMARY.md` when done, recording:
- Total per-file sections produced (counts split by .tsx vs .ts)
- Total translatable rows vs do-not-translate rows
- Top 5 files by string density (input to Phase 2 extraction ordering)
- Any sites that were ambiguous (e.g. could not decide between text-node and do-not-translate) — flag for human review
- The minimum-coverage checklist from RESEARCH.md §7 V7 with each item marked ✓
</output>
