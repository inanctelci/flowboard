---
phase: 05-data-model-migration-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/lib/character/schema.ts
  - frontend/src/lib/character/buildCharacterPrompt.ts
  - frontend/src/lib/character/toDataPatch.ts
  - frontend/src/lib/character/migrate.ts
  - frontend/src/store/board.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - MIGRATE-01
user_setup: []

must_haves:
  truths:
    - "Stable English keys (never translated display labels) flow into node.data via the new char* fields (DATA-05)"
    - "FlowboardNodeData accepts the 7 new flat char* keys and tsc -b --noEmit exits 0 (DATA-01)"
    - "A corrupt CharacterConfig blob fed to CharacterConfigSchema.safeParse() returns { success: false } and does not throw (DATA-02)"
    - "buildCharacterPrompt produces a deterministic prompt with no undefined/null/'unknown' tokens for any partial config (DATA-03)"
    - "toCharacterDataPatch(next, prev) emits ONLY changed keys; cleared fields → null sentinel; unchanged fields omitted; non-character keys never appear (DATA-04)"
    - "A v1.0 board with charCountry: 'vn' migrates to charEthnicity: 'Vietnamese' in-memory on BOTH loadInitialBoard AND switchBoard with zero backend PATCH writes (MIGRATE-01)"
    - "scripts/check-i18n-parity.mjs exits 0 (no new locale keys introduced)"
  artifacts:
    - path: "frontend/src/lib/character/schema.ts"
      provides: "CharacterConfigSchema (Zod), CharacterConfig type, VersionedCharacterConfigSchema, GenderKey, EthnicityKey, AgeKey, ExpressionKey, LightingKey, CHARACTER_CONFIG_VERSION"
      exports: ["CharacterConfigSchema", "CharacterConfig", "VersionedCharacterConfigSchema", "VersionedCharacterConfig", "CHARACTER_CONFIG_VERSION", "GenderKey", "EthnicityKey", "AgeKey", "ExpressionKey", "LightingKey"]
    - path: "frontend/src/lib/character/buildCharacterPrompt.ts"
      provides: "Pure prompt-assembler (DATA-03) with frozen FRAMING_ANCHORS constant and locked D-10 token order"
      exports: ["buildCharacterPrompt", "FRAMING_ANCHORS"]
    - path: "frontend/src/lib/character/toDataPatch.ts"
      provides: "Delta-only DataPatch emitter honoring null-sentinel contract (DATA-04)"
      exports: ["toCharacterDataPatch"]
    - path: "frontend/src/lib/character/migrate.ts"
      provides: "Pure idempotent convert-on-read migration charCountry → charEthnicity (MIGRATE-01)"
      exports: ["migrateCharacterNodeData", "LEGACY_COUNTRY_TO_ETHNICITY"]
    - path: "frontend/src/store/board.ts"
      provides: "FlowboardNodeData extended with 7 new char* fields; migrateCharacterNodeData wired into BOTH hydration sites"
      contains: "charEthnicity?: string"
    - path: "frontend/package.json"
      provides: "zod ^4 added to dependencies"
      contains: "\"zod\""
  key_links:
    - from: "frontend/src/store/board.ts (loadInitialBoard)"
      to: "frontend/src/lib/character/migrate.ts"
      via: "import + wrap data: { ... } in migrateCharacterNodeData(...)"
      pattern: "migrateCharacterNodeData\\("
    - from: "frontend/src/store/board.ts (switchBoard)"
      to: "frontend/src/lib/character/migrate.ts"
      via: "import + wrap data: { ... } in migrateCharacterNodeData(...) — SECOND insertion site"
      pattern: "migrateCharacterNodeData\\("
    - from: "frontend/src/lib/character/toDataPatch.ts"
      to: "frontend/src/api/client.ts"
      via: "import type { DataPatch }"
      pattern: "import type \\{ DataPatch \\} from \"../../api/client\""
    - from: "frontend/src/lib/character/buildCharacterPrompt.ts"
      to: "frontend/src/constants/character.ts"
      via: "import { CHARACTER_VIBES } for vibe token expansion only"
      pattern: "CHARACTER_VIBES"
    - from: "frontend/src/lib/character/migrate.ts"
      to: "frontend/src/constants/character.ts"
      via: "import { CHARACTER_COUNTRIES } — Phase 5 sole importer of this constant"
      pattern: "CHARACTER_COUNTRIES"
---

<objective>
Phase 5 — Data Model + Migration Foundation. Land the stable, locale-independent character data contract that every downstream phase (Phase 6 wizard, Phase 7 ResultViewer/constants removal) writes against.

Purpose: Six v1.1 requirements ship as a single atomic, mechanical drop. Per CONTEXT.md D-01..D-22 and RESEARCH.md §1–§7, the work is pure TypeScript: one `npm install zod`, four new pure-function modules under `frontend/src/lib/character/`, one in-place extension of `FlowboardNodeData`, and one wrapper call inserted into BOTH hydration sites (`loadInitialBoard` AND `switchBoard` — RESEARCH.md §6 calls this out as the single highest-risk gotcha of the phase).

Output: 4 new files in `frontend/src/lib/character/`, 1 modified store file (`board.ts`), 1 added runtime dep (`zod@^4`). Zero backend changes. Zero React/Zustand imports in any of the new lib modules. Zero new i18n keys. `tsc -b --noEmit` exits 0; `scripts/check-i18n-parity.mjs` exits 0 (no-change gate).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-data-model-migration-foundation/05-CONTEXT.md
@.planning/phases/05-data-model-migration-foundation/05-RESEARCH.md
@.planning/phases/05-data-model-migration-foundation/05-PATTERNS.md
@.planning/research/SUMMARY.md
@CLAUDE.md
@frontend/src/api/client.ts
@frontend/src/store/board.ts
@frontend/src/lib/storyboardPrompt.ts
@frontend/src/constants/character.ts
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install zod and create all four lib/character/ modules</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/lib/character/schema.ts, frontend/src/lib/character/buildCharacterPrompt.ts, frontend/src/lib/character/toDataPatch.ts, frontend/src/lib/character/migrate.ts</files>
  <action>
    Implements DATA-02, DATA-03, DATA-04, DATA-05, and the pure-function half of MIGRATE-01.

    Step A — install zod (per D-05, RESEARCH.md §Standard Stack).
    Run: `cd frontend && npm install zod@^4`
    Verify `frontend/package.json` "dependencies" now contains `"zod": "^4.x"` (any 4.x patch is acceptable; researcher confirmed 4.4.3 is current). Verify `frontend/package-lock.json` records the install. Do NOT install anything else — `zustand/middleware/persist` ships with Zustand 5 and is Phase 6 scope.

    Step B — create `frontend/src/lib/character/schema.ts` (per D-05/D-06/D-07/D-08, RESEARCH.md §2).
    Pure data module. NO React, NO Zustand imports — only `zod`.
    Exports (all named, no default — CLAUDE.md naming rules):
      - `CHARACTER_CONFIG_VERSION = 1 as const` (SCREAMING_SNAKE module-level constant)
      - String-union type aliases with `Key` suffix per project convention:
          * `GenderKey = "male" | "female" | "nonbinary"`
          * `EthnicityKey = "east-asian" | "southeast-asian" | "south-asian" | "middle-eastern" | "african" | "latin" | "caucasian" | "mixed"` (8 regional buckets; planner discretion per D's open question — use this set)
          * `AgeKey = "teenager" | "young-adult" | "adult" | "middle-aged" | "mature" | "senior"` (6 buckets, REQUIREMENTS.md FEATURES section)
          * `ExpressionKey = "neutral" | "soft-smile" | "confident" | "thoughtful" | "custom"`
          * `LightingKey = "soft-daylight" | "studio" | "golden-hour" | "cinematic" | "low-key"`
        These are TS-type guidance for the wizard's chip values — the schema fields themselves stay `z.string().optional()` so free-text overrides (e.g. user-typed ethnicity) parse cleanly.
      - `CharacterConfigSchema = z.object({ ... })` — every field `.optional()` per D-06:
          * `charGender: z.string().optional()`
          * `charEthnicity: z.string().optional()`
          * `charAge: z.string().optional()`
          * `charHair: z.string().optional()` (single composite string per D-04)
          * `charSkinTone: z.string().optional()`
          * `charVibe: z.string().optional()`
          * `charOutfit: z.string().optional()`
          * `charExpression: z.string().optional()`
          * `charLighting: z.string().optional()`
          * `charExtras: z.string().max(200).optional()` (200-char cap per WIZARD-03 / RESEARCH.md §2)
      - `type CharacterConfig = z.infer<typeof CharacterConfigSchema>` (schema is single source of truth per D-05)
      - `VersionedCharacterConfigSchema = z.object({ version: z.literal(CHARACTER_CONFIG_VERSION), data: CharacterConfigSchema })` (D-08 envelope)
      - `type VersionedCharacterConfig = z.infer<typeof VersionedCharacterConfigSchema>`
    Add a module-level comment block (storyboardPrompt.ts-style, ~4 lines) explaining: this is the single source of truth for character data; all fields optional by design; values are stable English keys / English prose — NEVER translated display labels (D-03); `.safeParse()` at every call boundary (D-07) — never let Zod throw to the React tree.

    Step C — create `frontend/src/lib/character/buildCharacterPrompt.ts` (per D-09/D-10/D-11/D-12, RESEARCH.md §3).
    Pure function, NO React/Zustand. Imports: `import type { CharacterConfig } from "./schema";` and `import { CHARACTER_VIBES } from "../../constants/character";` (CHARACTER_VIBES import is research-cleared in §3 — this restriction applies only to CHARACTER_COUNTRIES).
    Module-level frozen constant at top of file (SCREAMING_SNAKE, `readonly` + `as const`):
      ```
      const FRAMING_ANCHORS: readonly string[] = [
        "head and shoulders framing, centered composition, sharp focus on face",
        "strictly front-on orientation, no head tilt, no head turn, no profile angle, no three-quarter view, no over-the-shoulder pose",
        "no glasses, no hat, no mask, no occlusion, nothing covering the face",
        "photorealistic, ultra-detailed, consistent character reference",
      ] as const;
      ```
      These four strings are FROZEN — copy them verbatim from RESEARCH.md §3 (sourced from existing GenerationDialog.tsx:61-73 for A/B parity). Export `FRAMING_ANCHORS` as a named export so the Phase 7 grep gate / future refactors can find it.
    Module-level `AGE_TOKENS: Record<string, string>` mapping age keys → English descriptors (`teenager → "teenage"`, `young-adult → "young adult"`, etc., per RESEARCH.md §3).
    Local helper `resolveVibeTokens(vibe: string | undefined): readonly string[]` that looks up `CHARACTER_VIBES.find((v) => v.key === vibe)?.tokens ?? []`.
    Exported function:
      `export function buildCharacterPrompt(config: Partial<CharacterConfig>): string`
      Implements the locked D-10 token order — copy the pseudocode from RESEARCH.md §3 verbatim (subject anchor → pose anchors → hair → skin → outfit → vibe tokens → expression → lighting → extras → FRAMING_ANCHORS). Use `.filter(Boolean)` (D-11) to drop unset fields. Final return: `tokens.join(", ")` (delimiter `", "` per D-12).
    Anti-pattern guard: NO `undefined`/`null`/`"unknown"` strings may leak into the joined result — the `.filter(Boolean)` plus null-or-undefined-bypass in each token slot guarantees this. NO parameterization of `FRAMING_ANCHORS`.

    Step D — create `frontend/src/lib/character/toDataPatch.ts` (per D-13/D-14/D-15/D-16/D-17, RESEARCH.md §4).
    Pure function, NO React/Zustand. Imports: `import type { DataPatch } from "../../api/client";` (type-only, do not redeclare — RESEARCH.md §4) and `import type { CharacterConfig } from "./schema";`.
    Exported function name: `toCharacterDataPatch` (per D-17 planner discretion — picked to avoid ambiguity with potential future generic `toDataPatch` helpers).
      Signature: `export function toCharacterDataPatch(next: Partial<CharacterConfig>, prev: Partial<CharacterConfig>): DataPatch`
    Logic — copy RESEARCH.md §4 implementation:
      * Build the union key set from `Object.keys(next)` ∪ `Object.keys(prev)`, cast to `Set<keyof CharacterConfig>`.
      * For each key:
          - `nextVal === prevVal` → omit (D-15)
          - `nextVal` defined AND non-empty-string → `patch[key] = nextVal` (D-13)
          - else if `prevVal` was defined AND non-empty-string → `patch[key] = null` (D-14 null sentinel — NEVER `undefined`; client.ts:191-194 explains why `JSON.stringify` drops `undefined`)
          - else both empty → no-op
      * Return `patch`.
    Anti-pattern guards (D-16, PITFALL-1, CLAUDE.md anti-patterns):
      - Function NEVER reconstructs the full CharacterConfig — only iterates over union of provided keys.
      - Function operates ONLY on `CharacterConfig` fields by type — `mediaId`, `aiBrief`, `prompt`, `briefPlan`, `aspectRatio`, etc. are not in `CharacterConfig` and cannot appear in the output.
      - Cleared fields MUST emit `null`, not `undefined`. This is the explicit shallow-merge delete sentinel (client.ts:191-194 docblock).

    Step E — create `frontend/src/lib/character/migrate.ts` (per D-18/D-19/D-20/D-21/D-22, RESEARCH.md §5).
    Pure function, NO React/Zustand. Imports: `import { CHARACTER_COUNTRIES } from "../../constants/character";` (Phase 5's SOLE importer of CHARACTER_COUNTRIES — Phase 7 MIGRATE-02 deletes the constant) and `import type { FlowboardNodeData } from "../../store/board";`.
    Module-level lookup, SCREAMING_SNAKE per project convention:
      ```
      const LEGACY_COUNTRY_TO_ETHNICITY: Record<string, string> = Object.fromEntries(
        CHARACTER_COUNTRIES.map((c) => [c.key, c.tag]),
      );
      ```
      This produces exactly: `{ vn: "Vietnamese", jp: "Japanese", kr: "Korean", cn: "Chinese", th: "Thai", us: "American", fr: "French" }` (7 entries, per RESEARCH.md §5 mapping table).
      Export `LEGACY_COUNTRY_TO_ETHNICITY` as a named export so the Phase 7 cleanup grep gate can find it.
    Exported function:
      `export function migrateCharacterNodeData(data: FlowboardNodeData): FlowboardNodeData`
      Logic per D-22 idempotency contract:
        1. If `data.charEthnicity` is already set (truthy string) → return `data` unchanged (idempotency guard, also handles already-migrated nodes).
        2. If `data.charCountry` is missing/falsy → return `data` unchanged (nothing to migrate, e.g. video/image/storyboard nodes).
        3. Look up `const mapped = LEGACY_COUNTRY_TO_ETHNICITY[data.charCountry]`.
        4. If `!mapped` (unknown legacy country key) → return `data` unchanged (D-19 — no error, no-op).
        5. Otherwise return `{ ...data, charEthnicity: mapped }` — new object, never mutate input. `charCountry` is INTENTIONALLY preserved on the returned object (Phase 7 MIGRATE-02 handles its removal).
    Behavior contracts:
      - NEVER calls `patchNode` or any backend API (D-21 — convert-on-read, no automatic PATCH).
      - NEVER mutates the input object (return new object via spread).
      - `charVibe` is intentionally left as-is (D-20 — Phase 5 retains the existing 6-vibe key space).

    Across all five new files: named exports only (CLAUDE.md), relative imports only (no `@/` alias), 2-space indent, double-quoted strings, semicolons, trailing commas in multi-line literals — match existing project style (see `frontend/src/lib/storyboardPrompt.ts`).

    Reference docs: D-01..D-22 in `.planning/phases/05-data-model-migration-foundation/05-CONTEXT.md`. Full implementation snippets in `.planning/phases/05-data-model-migration-foundation/05-RESEARCH.md` §2–§5. Per-file pattern map in `.planning/phases/05-data-model-migration-foundation/05-PATTERNS.md`.
  </action>
  <verify>
    <automated>cd /Users/inanctelci/Development/flowboard/frontend &amp;&amp; npm run lint &amp;&amp; node -e "const fs=require('fs'); const required=['src/lib/character/schema.ts','src/lib/character/buildCharacterPrompt.ts','src/lib/character/toDataPatch.ts','src/lib/character/migrate.ts']; for(const f of required){ if(!fs.existsSync(f)){ console.error('MISSING',f); process.exit(1);} } const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); if(!pkg.dependencies.zod){ console.error('zod not in deps'); process.exit(1);} console.log('OK zod='+pkg.dependencies.zod);"</automated>
  </verify>
  <done>
    `npm install zod@^4` succeeded; `frontend/package.json` lists `zod` in dependencies at version `^4.x`; `frontend/package-lock.json` updated. Four new files exist at the paths above with the documented exports. `npm run lint` (i.e. `tsc -b --noEmit`) exits 0 with no errors and no warnings about unused imports. No new files outside `frontend/src/lib/character/` (and the package.json/lock.json bumps). No edits to `agent/flowboard/` or `extension/`. No `export default`. No `@/` alias imports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend FlowboardNodeData and wire migrateCharacterNodeData into BOTH hydration sites</name>
  <files>frontend/src/store/board.ts</files>
  <action>
    Implements DATA-01 and the wiring half of MIGRATE-01. This is the SECOND-most-important task in Phase 5 — RESEARCH.md §6 names the dual-hydration insertion as the single highest-risk gotcha. DO NOT MISS THE SECOND SITE.

    Step A — add the migration import at the top of `frontend/src/store/board.ts`.
    After the existing imports from `../api/client` (around line 16), add a single new import line:
      `import { migrateCharacterNodeData } from "../lib/character/migrate";`
    Relative path only — match existing `../api/client` style. No `@/` alias.

    Step B — extend the `FlowboardNodeData` interface (currently lines 31–97).
    Insert seven new optional string fields immediately after `charGender?: string;` (currently line 88). Match the comment-block style of the existing `charCountry`/`charVibe`/`charGender` block (lines 82-88):
      ```
      // v1.1 character-builder structured fields (Phase 5 DATA-01) —
      // stable English keys / English prose stored on node.data; never
      // translated display labels (locale-independent boards). Legacy
      // charCountry/charVibe/charGender above are retained for backward
      // read access until Phase 7 (MIGRATE-02) removes them.
      charEthnicity?: string;
      charAge?: string;
      charHair?: string;
      charSkinTone?: string;
      charOutfit?: string;
      charExpression?: string;
      charLighting?: string;
      ```
    Per D-02: KEEP the existing `charCountry?: string;`, `charVibe?: string;`, `charGender?: string;` fields on the interface. Do NOT remove them — Phase 7 MIGRATE-02 owns their removal.
    Do NOT add `charExtras?: string` to the interface in this task — per RESEARCH.md §Open Questions Q2, `charExtras` is currently dialog-local in GenerationDialog and NOT persisted to node.data. Phase 6's wizard owns introducing it. Adding it here adds a hydration cast (next step) for a field that no row carries — leave it out to keep blast radius minimal.

    Step C — wire `migrateCharacterNodeData` into the `loadInitialBoard` hydration path (currently lines 264–288).
    The current `.map()` produces a node with `data: { type: n.type, ..., charGender, storyboardGrid }`. Two changes:
      (1) After the existing `charGender: n.data["charGender"] as string | undefined,` cast (line 285), add 7 new cast lines following the identical `n.data["KEY"] as string | undefined,` pattern:
        ```
        charEthnicity: n.data["charEthnicity"] as string | undefined,
        charAge: n.data["charAge"] as string | undefined,
        charHair: n.data["charHair"] as string | undefined,
        charSkinTone: n.data["charSkinTone"] as string | undefined,
        charOutfit: n.data["charOutfit"] as string | undefined,
        charExpression: n.data["charExpression"] as string | undefined,
        charLighting: n.data["charLighting"] as string | undefined,
        ```
        Place them between `charGender` and `storyboardGrid` so the order of properties mirrors the FlowboardNodeData interface order.
      (2) Wrap the entire `data: { ... }` object in `migrateCharacterNodeData(...)`. The shape becomes:
        ```
        data: migrateCharacterNodeData({
          type: n.type,
          shortId: n.short_id,
          // ... all existing casts ...
          charGender: n.data["charGender"] as string | undefined,
          charEthnicity: n.data["charEthnicity"] as string | undefined,
          charAge: n.data["charAge"] as string | undefined,
          charHair: n.data["charHair"] as string | undefined,
          charSkinTone: n.data["charSkinTone"] as string | undefined,
          charOutfit: n.data["charOutfit"] as string | undefined,
          charExpression: n.data["charExpression"] as string | undefined,
          charLighting: n.data["charLighting"] as string | undefined,
          storyboardGrid: n.data["storyboardGrid"] as StoryboardGrid | undefined,
        }),
        ```
        `migrateCharacterNodeData` accepts a `FlowboardNodeData` and returns the same shape — the outer `{ id, type, position, data }` structure is unchanged.

    Step D — REPEAT Step C verbatim in `switchBoard` (currently lines 320–344).
    This is the same `.map()` pattern in a second function. RESEARCH.md §6 / Pitfall 1: missing `switchBoard` means old boards migrate correctly on first load but REVERT when the user switches away and back. Add the same 7 cast lines in the same relative position, then wrap `data: { ... }` in `migrateCharacterNodeData(...)` identically.
    Verification after this step: `grep -c "migrateCharacterNodeData" frontend/src/store/board.ts | grep -v '^#'` MUST return a count of at least 3 (1 import + 2 call sites). The verify command below enforces this.

    Behavior contracts honored:
      - No automatic PATCH back to the backend (D-21). `migrateCharacterNodeData` is read-only.
      - `charCountry`, `charVibe`, `charGender` casts are RETAINED (D-02).
      - Backend `Node.data` JSON column is untouched on read; only the in-memory Zustand store sees the migrated shape.
      - No other functions in `board.ts` are modified — write paths (`updateNode`, `patchNode` callers) do not need the migration call because they operate on already-hydrated `FlowNode` instances.

    Reference docs: D-01, D-02, D-21, D-22 in 05-CONTEXT.md; RESEARCH.md §6 (exact line numbers and worked snippets); PATTERNS.md "Modified: board.ts" section.
  </action>
  <verify>
    <automated>cd /Users/inanctelci/Development/flowboard/frontend &amp;&amp; npm run lint &amp;&amp; node -e "const fs=require('fs'); const src=fs.readFileSync('src/store/board.ts','utf8'); const imp=(src.match(/import \{ migrateCharacterNodeData \} from \"\.\.\/lib\/character\/migrate\"/g)||[]).length; const calls=(src.match(/migrateCharacterNodeData\(\{/g)||[]).length; if(imp!==1){console.error('Expected 1 import of migrateCharacterNodeData, got '+imp); process.exit(1);} if(calls!==2){console.error('Expected 2 call sites of migrateCharacterNodeData (loadInitialBoard AND switchBoard), got '+calls); process.exit(1);} const newKeys=['charEthnicity','charAge','charHair','charSkinTone','charOutfit','charExpression','charLighting']; for(const k of newKeys){ const interfaceHit=new RegExp('  '+k+'\\\\?: string;').test(src); const castHits=(src.match(new RegExp(k+': n.data\\\\[\"'+k+'\"\\\\] as string \\\\| undefined,','g'))||[]).length; if(!interfaceHit){console.error('Missing interface field: '+k); process.exit(1);} if(castHits!==2){console.error('Expected 2 hydration casts for '+k+', got '+castHits); process.exit(1);} } if(!/charCountry\\?: string;/.test(src)){console.error('Legacy charCountry was removed — must stay per D-02'); process.exit(1);} if(!/charVibe\\?: string;/.test(src)){console.error('Legacy charVibe was removed — must stay per D-02'); process.exit(1);} console.log('OK: 1 import, 2 call sites, 7 new interface fields, 14 hydration casts, legacy keys preserved.');" &amp;&amp; node ../scripts/check-i18n-parity.mjs</automated>
  </verify>
  <done>
    `frontend/src/store/board.ts` has:
      (a) one new import line `import { migrateCharacterNodeData } from "../lib/character/migrate";`
      (b) 7 new optional fields on `FlowboardNodeData` (`charEthnicity`, `charAge`, `charHair`, `charSkinTone`, `charOutfit`, `charExpression`, `charLighting`), each `?: string`
      (c) legacy `charCountry`, `charVibe`, `charGender` fields RETAINED on the interface (D-02)
      (d) 7 new `n.data["KEY"] as string | undefined,` cast lines in `loadInitialBoard` AND 7 identical cast lines in `switchBoard` (14 new casts total)
      (e) the `data: { ... }` literal wrapped in `migrateCharacterNodeData(...)` in BOTH `loadInitialBoard` AND `switchBoard` (exactly 2 call sites — single-site is a P0 regression).
    `npm run lint` exits 0. `node scripts/check-i18n-parity.mjs` exits 0 (no new locale keys → no-change gate). No edits to any file other than `frontend/src/store/board.ts` in this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → Zustand store (deferred to Phase 6) | Phase 5 ships the schema + safeParse contract that will guard this boundary; no consumer yet. |
| Backend JSON column → frontend Zustand store | `n.data` is `Record<string, unknown>` from the API — every read goes through TS casts; migrate.ts adds a normalizing shim for legacy keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Persisted `node.data` blob returned by GET `/api/boards/{id}` carries an unknown / malformed `charCountry` key | mitigate | `migrateCharacterNodeData` falls through to no-op on any value not in the 7-key `LEGACY_COUNTRY_TO_ETHNICITY` map (D-19); no exceptions thrown; node renders with legacy fields visible |
| T-05-02 | Tampering | Future localStorage corruption of `CharacterConfig` blob (Phase 6 surface, schema authored here) | mitigate | `CharacterConfigSchema.safeParse()` returns `{ success: false }` on malformed input (D-07); never throws to React tree; routes failure to a store `error` slot via `console.warn` (pattern enforced by D-07 / RESEARCH.md §2 usage contract). Phase 5 ships the schema; Phase 6 wires the consumer. |
| T-05-03 | Information Disclosure | Translated display labels (Vietnamese / Turkish / English locale strings) accidentally stored in `node.data.charEthnicity` instead of stable English keys | mitigate | D-03 / DATA-05 locks the contract; `migrateCharacterNodeData` writes only values from `CHARACTER_COUNTRIES[n].tag` (English by definition); new lib modules import no i18n helpers (RESEARCH.md §1 import table); Phase 6/7 reviews enforce the same rule at the wizard surface. |
| T-05-04 | Tampering | Wholesale-replace of `node.data` PATCH wipes `mediaId` / `aiBrief` / `prompt` (CLAUDE.md anti-pattern #1, client.ts:196-219) | mitigate | `toCharacterDataPatch` operates strictly over `Partial<CharacterConfig>` typed keys (DATA-04); cleared fields emit `null` sentinel (D-14); unchanged fields omitted (D-15); function signature TYPE-prevents non-character keys from appearing in output (D-16). Phase 6 wizard MUST use this helper — Phase 5 ships the helper, Phase 7 grep gate enforces no other PATCH-construction sites. |
| T-05-05 | Repudiation | Migration silently overwrites a user-curated `charEthnicity` value during a v1.0→v1.1 transition (data loss without user signal) | mitigate | D-22 idempotency guard: if `charEthnicity` already set (truthy), `migrateCharacterNodeData` returns input unchanged on FIRST line — no overwrite path exists. Truth table in 05-RESEARCH.md §5 verifies all five edge cases. |
| T-05-SC | Tampering | npm install of `zod@^4` introduces malicious dep / typosquat | mitigate | RESEARCH.md §"Package Legitimacy Audit" confirms `zod` is OK (16M+ weekly downloads, official at zod.dev, 5+ years old, registry-verified). Disposition: approved. Single package install; no transitive deps added by zod (it has zero runtime deps). |
</threat_model>

<verification>
Phase-close verification — observable, automated where possible:

1. `cd frontend && npm run lint` → exits 0 (strict TS clean across all 5 changed files).
2. `cd frontend && node ../scripts/check-i18n-parity.mjs` → exits 0 (Phase 5 introduces zero new locale keys; this is a no-change gate).
3. `grep -c "migrateCharacterNodeData" frontend/src/store/board.ts` → ≥ 3 (1 import + 2 call sites). Filter for non-comment occurrences with `grep -v "^#"` if needed — `board.ts` has no `#` comments so the raw count is safe.
4. `grep -rn "CHARACTER_COUNTRIES" frontend/src/lib/character/` → exactly 1 hit (`migrate.ts` is the sole importer per D-19 / RESEARCH.md §1).
5. `grep -rn "CHARACTER_COUNTRIES" frontend/src/` → existing uses in `constants/character.ts` (the file itself), `components/GenerationDialog.tsx` (Phase 6 will touch), and `lib/character/migrate.ts` only. No new use sites introduced.
6. Open the dev server (`make agent` in one shell, `make frontend` in another), open a v1.0 board that has a character node with persisted `charCountry: "vn"`, and inspect the board store (React DevTools or `useBoardStore.getState().nodes`): the character node's `data.charEthnicity` is `"Vietnamese"`, `data.charCountry` is still `"vn"` (preserved on backend; cast lives), Network tab shows ZERO `PATCH /api/nodes/*` requests fired by the load (D-21 — convert-on-read only). Capture this in `05-SUMMARY.md`.
7. Browser console A/B parity micro-check (capture diff in SUMMARY.md):
   - Old: in `components/GenerationDialog.tsx`, the current `buildCharacterPrompt("female", "vn", "clean", "")` (still present until Phase 6).
   - New: `(await import("/src/lib/character/buildCharacterPrompt.ts")).buildCharacterPrompt({ charGender: "female", charEthnicity: "Vietnamese", charVibe: "clean" })`
   - Expected: morally identical token order, framing anchors at the same fixed positions, identical delimiter `", "`. Capture the literal diff in `05-SUMMARY.md` (MIGRATE-04 will re-use this baseline in Phase 7).
8. Browser console truth-table sanity (`toCharacterDataPatch`):
   - `toCharacterDataPatch({ charVibe: "douyin" }, { charVibe: "clean", charGender: "female" })` → `{ charVibe: "douyin" }` (DATA-04 single-field-change worked example)
   - `toCharacterDataPatch({ charGender: "female" }, { charOutfit: "business casual", charGender: "female" })` → `{ charOutfit: null }` (DATA-04 cleared-field null-sentinel worked example)
   - `toCharacterDataPatch({ charGender: "female" }, { charGender: "female" })` → `{}` (DATA-04 noop worked example)
9. Browser console schema safeParse check:
   - `(await import("/src/lib/character/schema.ts")).CharacterConfigSchema.safeParse({ charExtras: "x".repeat(201) }).success` → `false` (DATA-02 boundary check, 200-char cap enforced).
   - `(await import("/src/lib/character/schema.ts")).CharacterConfigSchema.safeParse({ randomKey: 42 }).success` → `true` (extra keys allowed by default — Zod `.object()` strips them; behavior is acceptable for tolerance of partial blobs per D-06).
</verification>

<success_criteria>
1. `FlowboardNodeData` accepts the 7 new flat `char*` fields (`charEthnicity`, `charAge`, `charHair`, `charSkinTone`, `charOutfit`, `charExpression`, `charLighting`), all `?: string`. Legacy `charCountry`, `charVibe`, `charGender` remain (D-02). `tsc -b --noEmit` exits 0 with zero new errors and zero unused-import warnings. ⟶ DATA-01.
2. `CharacterConfigSchema.safeParse({ charExtras: "x".repeat(201) }).success === false`; `CharacterConfigSchema.safeParse({}).success === true`; `CharacterConfigSchema.safeParse({ charGender: "female", charEthnicity: "Vietnamese" }).success === true`. Schema NEVER throws when fed arbitrary input (D-07). ⟶ DATA-02 + DATA-05.
3. `buildCharacterPrompt({ charGender: "female", charEthnicity: "Vietnamese", charVibe: "clean" })` returns a deterministic string containing the four `FRAMING_ANCHORS` clauses in order, no `undefined`, no `null`, no `"unknown"` substrings. Same call repeated returns byte-identical output. `buildCharacterPrompt({})` returns a non-empty string starting with `"Studio portrait headshot of a person character"` (fallback subject) and ending with the four framing anchors — never `undefined`. ⟶ DATA-03.
4. **Delta truth table** (DATA-04): `toCharacterDataPatch({ charVibe: "douyin" }, { charVibe: "clean", charGender: "female", charEthnicity: "Vietnamese" })` returns EXACTLY `{ charVibe: "douyin" }`. The returned object MUST NOT contain `mediaId`, `aiBrief`, `prompt`, `briefPlan`, `aspectRatio`, `charGender`, `charEthnicity`, or any other field. Cleared field test: `toCharacterDataPatch({ charGender: "female" }, { charGender: "female", charOutfit: "business casual" })` returns EXACTLY `{ charOutfit: null }`. Noop test: `toCharacterDataPatch({ charGender: "female" }, { charGender: "female" })` returns EXACTLY `{}`. ⟶ DATA-04.
5. A v1.0 board with a character node carrying `charCountry: "vn"` / `charVibe: "clean"` / no `charEthnicity` is loaded via `loadInitialBoard` AND, after navigating away and back, via `switchBoard`. In BOTH cases the in-memory `node.data` has `charEthnicity: "Vietnamese"`, `charVibe: "clean"` (preserved), `charCountry: "vn"` (preserved). Network tab shows ZERO `PATCH /api/nodes/*` requests fired during the load. ⟶ MIGRATE-01.
6. `node scripts/check-i18n-parity.mjs` exits 0. ⟶ Phase 5 no-change i18n gate.
7. No files modified outside `frontend/package.json`, `frontend/package-lock.json`, `frontend/src/lib/character/{schema,buildCharacterPrompt,toDataPatch,migrate}.ts`, and `frontend/src/store/board.ts`. No `agent/flowboard/` or `extension/` changes (CLAUDE.md milestone constraint). No new i18n keys in `en.json` / `tr.json`. ⟶ Phase 5 scope boundary.
</success_criteria>

<output>
After both tasks complete and all verification checks pass, create `.planning/phases/05-data-model-migration-foundation/05-01-SUMMARY.md` per the standard summary template. Required fields:

- **Decisions implemented:** D-01 through D-22 (all 22 locked decisions from 05-CONTEXT.md).
- **Requirements covered:** DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, MIGRATE-01 (all 6 phase requirements).
- **Files changed:** `frontend/package.json` (zod added), `frontend/package-lock.json` (regenerated), `frontend/src/lib/character/schema.ts` (NEW), `frontend/src/lib/character/buildCharacterPrompt.ts` (NEW), `frontend/src/lib/character/toDataPatch.ts` (NEW), `frontend/src/lib/character/migrate.ts` (NEW), `frontend/src/store/board.ts` (MODIFIED — interface + 2 hydration sites).
- **A/B parity note (MIGRATE-04 baseline):** capture the literal v1.0 vs. v1.1 prompt string diff for input `{ charGender: "female", charEthnicity: "Vietnamese", charVibe: "clean" }`. Phase 7 MIGRATE-04 reads this baseline.
- **Manual verification log:** record the browser-console results of verification steps 6, 7, 8, 9 from the `<verification>` block above.
- **Open items for Phase 6:** `charExtras` is in the schema but NOT yet cast in `board.ts` hydration — Phase 6 WIZARD-03 introduces persistence and the hydration cast lands in that phase; `characterPresets.ts` slice + persist middleware wiring; consumers of `CharacterConfigSchema.safeParse()` (none in Phase 5).
- **Patterns to follow / avoid:** restate the four lib modules' "no React, no Zustand imports" invariant; restate the dual-hydration-site rule (Pitfall 1 from RESEARCH.md §7).
</output>
